# Root requirements (used only by the build stage)
# No direct dependencies at the root level – each service has its own
# requirements file.  The file is kept for future expansion or
# manual pip install outside of Docker.

# Example:
# pip install -r requirements.txt