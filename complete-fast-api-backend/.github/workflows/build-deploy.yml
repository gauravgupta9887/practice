name: Build and Deploy Code

on:
    push:
        branches:
            - "main"
            - "another_branch"
            - "feature"
    pull_request:
        branches:
            - "main"
            - "another_branch"
            - "feature"

jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
            - name: pulling git repo
              uses: actions/checkout@v2
            - name: say hi to gaurav
              run: echo "hi gaurav"
