version: "3"
services:
  api:
    build: .
    ports:
      # - <port on localhost>:<port on container>
      - 8000:8000
    env_file:
      - ./.env
    environment:
      - DATABASE_HOSTNAME=postgres
      - DATABASE_PORT=5432
